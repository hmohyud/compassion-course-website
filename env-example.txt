# Database Configuration
MONGODB_URI=mongodb://localhost:27017/compassion-course
# For production, use MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/compassion-course

# JWT Secret (generate a secure random string)
JWT_SECRET=your-super-secure-jwt-secret-key-change-this-in-production

# Server Configuration
PORT=3000
NODE_ENV=development

# Admin Account (for initial setup)
ADMIN_EMAIL=admin@compassioncourse.com
ADMIN_PASSWORD=change-this-secure-password

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File Upload Configuration
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/gif,image/webp

# Frontend (Vite) - used by the React app
# Jotform: the Form ID of the form to show in the hero popup (get it from the form URL: form.jotform.com/XXXXXXXXXX)
# Do NOT put your Jotform login password here; the site only embeds the form by ID.
VITE_JOTFORM_FORM_ID=260333329475357

# Backlog embed (Member Hub /platform/resources): full URL of the thatagileapp instance to embed.
# If thatagileapp.com does not load, set this to your Netlify URL, e.g. https://thatagileapp.netlify.app/the-compassion-course?embed=1
# VITE_BACKLOG_EMBED_URL=https://thatagileapp.com/the-compassion-course?embed=1
